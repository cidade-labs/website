---
import Base from '../../../layouts/Base.astro';
import { getCollection } from 'astro:content';
import { useTranslations } from '../../../i18n/ui.js';

/* This dynamic route generates one page per English blog post.
   getStaticPaths tells Astro which slugs exist. */
export async function getStaticPaths() {
  const posts = await getCollection('blog', (p) => p.data.lang === 'en' && !p.data.draft);
  return posts.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const lang = 'en';
const t = useTranslations(lang);
const { post } = Astro.props;
const { Content } = await post.render();

const dateStr = new Date(post.data.pubDate).toLocaleDateString('en', {
  year: 'numeric', month: 'long', day: 'numeric',
});
---
<Base lang={lang} active="blog" title={`${post.data.title} · Cidade Labs`} description={post.data.description}>
  <article class="article">
    <div class="wrap">
      <div class="article-head">
        <p class="kicker">{t('blog.title')}</p>
        <h1>{post.data.title}</h1>
        <p class="byline">{post.data.author} · {dateStr}</p>
      </div>
      <div class="article-body">
        <Content />
      </div>
    </div>
  </article>
</Base>
