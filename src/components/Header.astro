---
import Mark from './Mark.astro';
import Controls from './Controls.astro';
import { localizePath } from '../i18n/ui.js';
const { lang = 'gl' } = Astro.props;
---
<header class="reader-mast wrap"><a class="small-brand" href={localizePath('/',lang)}><Mark />Cidade Labs</a><Controls lang={lang} /></header>
